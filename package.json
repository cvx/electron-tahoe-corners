{
  "name": "electron-tahoe-corners",
  "description": "Enable macOS Tahoe toolbar-style rounded corners",
  "version": "1.0.0",
  "keywords": [
    "electron"
  ],
  "main": "index.js",
  "scripts": {
    "configure": "node-gyp configure",
    "debug": "node-gyp rebuild --debug",
    "build": "node-gyp rebuild --release",
    "start": "npm run build && cd demo && npm start"
  },
  "dependencies": {
    "bindings": "^1.5.0"
  }
}
